
export enum ReportType {
    RISK = 'risk',
    COMPLIANCE = 'compliance',
    VULN = 'vuln',
    COMBINED = 'combined',
}

export enum ReportStatus {
    DRAFT = 'draft',
    IN_REVIEW = 'in_review',
    FINAL = 'final',
}

export enum PostureLevel {
    LOW = 'low',
    MODERATE = 'moderate',
    HIGH = 'high',
    CRITICAL = 'critical',
}